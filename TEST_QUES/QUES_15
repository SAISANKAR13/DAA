def count_words_with_prefix(words, pref):
    count = 0
    for word in words:
        if word.startswith(pref):
            count += 1
    return count

words = ["pay", "attention", "practice", "attend"]
pref = "at"
print("Number of words with prefix 'at':", count_words_with_prefix(words, pref))
