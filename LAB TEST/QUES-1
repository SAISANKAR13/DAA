def removeElement(nums, val):
    k = 0               
    for num in nums:
        if num != val:
            nums[k] = num
            k += 1
    return k

# Example usage:
nums = [3, 2, 2, 3]
val = 3
k = removeElement(nums, val)
print(k, nums[:k])
